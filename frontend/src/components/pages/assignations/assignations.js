import {Container} from 'react-bootstrap'

function Assignations() {
    return(
        <Container fluid>
            <Container>
                This is the assignations page.
            </Container>
        </Container>
    )
}

export default Assignations;